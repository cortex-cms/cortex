default: &default
  after_rails:
    - command: /.cloud66/bootstrap.sh
      target: rails
      sudo: true
    - source: /.cloud66/mongoid.yml
      destination: $STACK_PATH/config/mongoid.yml
      target: rails

production:
  <<: *default

development:
  <<: *default

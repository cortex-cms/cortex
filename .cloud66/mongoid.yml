default: &default
  sessions:
    default:
      database: <%= ENV['MONGODB_DATABASE']%>
      hosts: <%= "[\"#{ENV['MONGODB_ADDRESS']}:27017\"]" %>

development:
  <<: *default

production:
  <<: *default

staging:
  <<: *default

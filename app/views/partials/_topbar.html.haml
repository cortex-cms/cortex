%header.mdl-layout__header
  // TODO removed .demo-header, does that break things
  .mdl-layout__header-row
    = render 'partials/breadcrumb'
    .mdl-layout-spacer
    = render 'partials/user_control'

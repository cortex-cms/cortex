<%= form_for(@platform_admin) do |f| %>
  <% if @platform_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@platform_admin.errors.count, "error") %> prohibited this platform_admin from being saved:</h2>

      <ul>
      <% @platform_admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.number_field :organization_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

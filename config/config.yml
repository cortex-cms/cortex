defaults: &defaults
    default_subdomain: development
    allowed_origins:
      - 'localhost:3000'
      - 'localhost:63342'

development:
    <<: *defaults

test:
    <<: *defaults

production:
    <<: *defaults